<?xml version="1.0" encoding="utf-8"?>
<!-- Dialog view: circular avatar + yellow edit FAB + white outlined TextInput -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/edit_profile_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Avatar with stroke like in setup_profile + overlay FAB -->
    <FrameLayout
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_gravity="center_horizontal">

        <com.google.android.material.card.MaterialCardView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            app:cardBackgroundColor="@color/bg_grey"
            app:cardCornerRadius="@dimen/AddDog_img_corners_100dp"
            app:cardElevation="@dimen/cardview_default_elevation"
            app:strokeColor="@color/Secondary_pink"
            app:strokeWidth="@dimen/AddDog_img_stroke_4dp">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/edit_img_avatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@string/edit_profile_image"
                android:scaleType="centerCrop"
                app:srcCompat="@drawable/user_default_img" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Yellow edit FAB (uses existing camera_ic; replace with ic_edit if you have it) -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/edit_btn_change_photo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="2dp"
            android:contentDescription="@string/change_photo"
            app:backgroundTint="@color/Primary_yellow"
            app:fabSize="mini"
            app:maxImageSize="32dp"
            app:srcCompat="@drawable/camera_ic"
            app:tint="@android:color/black" />
    </FrameLayout>

    <!-- Display name (white outlined input like setup screen) -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/edit_til_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        app:boxBackgroundMode="outline"
        app:boxBackgroundColor="@color/white"
        app:boxCornerRadiusTopStart="12dp"
        app:boxCornerRadiusTopEnd="12dp"
        app:boxCornerRadiusBottomStart="12dp"
        app:boxCornerRadiusBottomEnd="12dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edit_edt_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/display_name"
            android:padding="@dimen/margin_m" />
    </com.google.android.material.textfield.TextInputLayout>
</LinearLayout>
